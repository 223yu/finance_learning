$('.entry__index-tbody').append('<%= j(render 'create', journal: @journal) %>');
$('.single-entry__new').html('<%= j(render 'new', journal: @journal_new) %>');
<!-- create後、autofocusが効かなくなるのでjqueryで対応 -->
$('#journal_month').focus();
<!-- table最下部までスクロール -->
$('.entry__index-tbody').animate({scrollTop: $('.entry__index-tbody')[0].scrollHeight}, 'fast');